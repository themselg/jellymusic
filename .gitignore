*.iml
.gradle/
.kotlin/
/local.properties
/.idea/
.DS_Store
/build
/app/build
# Module build dirs (anywhere, incl. build-logic and future :core/:feature modules)
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Signing material — never commit the keystore or the credentials.
*.keystore
keystore.properties
